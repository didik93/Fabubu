Fabubu 安装简易指南

[Windows 用户]
直接双击运行安装包即可。如有安全提示，点击“更多信息” -> “仍要运行”。

[macOS 用户]
1. 双击 .dmg 文件，将 Fabubu 拖入应用程序文件夹。
2. 首次运行如果提示“已损坏”或“无法打开”，请执行以下操作：
   打开终端(Terminal)，复制并运行以下命令(需要输入开机密码)：

   sudo xattr -r -d com.apple.quarantine /Applications/Fabubu.app

   运行后重新打开 APP 即可。
